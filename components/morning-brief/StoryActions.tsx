"use client";

import { Bookmark, Heart, MoreHorizontal } from "lucide-react";
import { useState } from "react";
import { Dialog } from "@/components/ui/Dialog";

export function StoryActions() { const [liked, setLiked] = useState(false); const [saved, setSaved] = useState(false); const [menuOpen, setMenuOpen] = useState(false); const buttonClass = "grid h-10 w-10 place-items-center rounded-full text-text-secondary transition-colors hover:bg-sand-200 hover:text-text-primary"; return <div className="flex shrink-0 items-center"><button type="button" className={buttonClass} aria-label={liked ? "Unlike article" : "Like article"} aria-pressed={liked} onClick={() => setLiked((value) => !value)}><Heart size={18} className={liked ? "fill-accent text-accent" : ""} /></button><button type="button" className={buttonClass} aria-label={saved ? "Remove saved article" : "Save article"} aria-pressed={saved} onClick={() => setSaved((value) => !value)}><Bookmark size={18} className={saved ? "fill-accent text-accent" : ""} /></button><button type="button" className={buttonClass} aria-label="More article actions" onClick={() => setMenuOpen(true)}><MoreHorizontal size={19} /></button><Dialog open={menuOpen} onClose={() => setMenuOpen(false)} title="Article options"><div className="divide-y divide-border-subtle">{["Save for later", "Not relevant", "Show fewer like this", "Why am I seeing this?"].map((label) => <button key={label} type="button" className="block w-full py-3 text-left text-sm text-text-primary hover:text-accent" onClick={() => setMenuOpen(false)}>{label}</button>)}</div></Dialog></div>; }
